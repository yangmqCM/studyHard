import { FlyHeroesImpurePipe } from './fly-heroes-impure.pipe';

describe('FlyHeroesImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyHeroesImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
